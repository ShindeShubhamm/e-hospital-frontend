import { createMuiTheme } from '@material-ui/core/styles';

export const AppTheme = createMuiTheme({
  palette: {
    primary: {
      main: '#06214f',
    },
    secondary: {
      main: '#f3b239',
    },
    success: {
      main: '#51b651',
    },
    error: {
      main: '#d96565',
    },
    type: 'light',
  },
});
